{
  "name": "markdown-editor",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon app.js",
    "server": "node app.js",
    "builds":"npm install --lrgacy-peer-deps && npm install --legacy-peer-deps --prefix frontend && npm run build --prefix frontend",
    "build": "npm install --legacy-peer-deps && cd ../frontend && npm install --legacy-peer-deps && npm run build"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "helmet": "^7.1.0",
    "http": "^0.0.1-security",
    "markdown-it": "^14.1.0",
    "nodemon": "^3.1.0",
    "socket.io": "^4.7.5"
  }
}
